# Safe Disk 跨平台验收清单

> 本文件只收录：当前 Linux 主线已经验收、代码和自动化测试已经存在，但仍需 Windows、macOS 或独立桌面环境复验的事项。它不是完成档案，也不替代功能设计和缺陷任务。
>
> 最后整理：2026-07-23。

## 使用规则

- 每项必须记录操作系统、版本、缩放比、应用 bundle/动态库版本、实际步骤和结果。
- Windows/macOS 的构建或交叉编译只能作为前置证据，不能替代实际 GUI、CLI、FFI 验收。
- 在本清单验证失败时，创建新的活跃 bug 回到 [TODO.md](TODO.md)；不能直接在本表把失败写成“已完成”。
- 三个平台均有满足范围的实际证据后，才可迁入 [已完成任务](completed/TASKS_COMPLETED.md)。

## Windows

| 原 ID | 验收范围 | 当前代码/测试证据 | Windows 实机待验证 |
|---|---|---|---|
| UI-02、UI-23 | 创建加密 root、空目录与不存在目录语义 | DLL 规范名、非空目录拒绝和 sec/FFI 测试已存在 | Win10 新 bundle 下验证不存在/空目录成功、非空目录拒绝不改动数据、盘符和权限拒绝信息。 |
| UI-22 | 字体和缩放清晰度 | PerMonitorV2、Segoe UI 与中文回退已配置 | Win10 100%/125%/150% 截图及肉眼对比主窗、记事本和图片子窗。 |
| UI-24、UI-37 | 动态库与详细错误诊断 | 错误分层、脱敏和启动失败页已有 widget/Linux 实测 | 缺失或错误 DLL 时验证默认不泄露详情、开启后绝对路径仍脱敏、复制信息可用于定位。 |
| UI-27 | 向上导航和 root 面包屑 | Windows 路径规范化 unit/widget 已覆盖 | 深层目录首次向上、盘符、别名 root 和相似前缀边界。 |
| UI-33、SEC-01 | 高级加密组合和旧算法名 root | 真实 FFI/CLI 组合与旧 root 读取已覆盖 | 公开组合创建、写入、重开，以及三份旧 root 的 Dart FFI 读取。 |
| UI-34 | 桌面快捷键 | widget 已覆盖 F2/F5/Ctrl+V/Ctrl+F/Ctrl+S | 真实键位、菜单提示和输入控件优先级。 |
| UI-75 | 外部拖放 Windows 路径边界 | `DragDropController` 已在注入 Windows 规则下统一 `/` 与 `\\` 分隔符并做大小写不敏感 root 边界判断；Linux controller/HomeShell 定向回归通过 | Windows 资源管理器多文件/目录拖入、盘符和 UNC 路径、权限拒绝、当前 root 内部路径拒绝及临时文件生命周期。 |
| UI-38、UI-42、UI-47、UI-49、UI-51 | 安全记事本查找、撤销重做、数字键盘 Enter 与脱敏错误 | 查找 overlay、标准 Ctrl/Cmd 快捷键、数字键盘 Enter、只读边界和脱敏错误 widget/controller 回归均已存在 | 在 100%/125%/150% 缩放下验证查找命中可见居中与选中高亮、Ctrl+F/Ctrl+Z/Ctrl+Shift+Z、普通/数字键盘 Enter、关闭/草稿/剪贴板错误的脱敏展示及读屏状态。 |
| UI-40、UI-45、UI-50 | 图片查看器滚轮缩放与状态语义 | widget 已证明工具栏与鼠标滚轮共用 `10%–1000%` 缩放范围，并覆盖加载、拒绝、失败和已打开图片的 live region 语义 | 鼠标滚轮、触控板、高 DPI 下缩放下限、旋转、翻页和读屏状态。 |
| UI-55 | 属性值选择与复制 | widget 覆盖安全值系统剪贴板复制、反馈和敏感字段无复制入口 | 鼠标选择、Ctrl+C、复制按钮焦点/读屏标签，以及高 DPI 文本截断。 |
| UI-58 | 跨 root 目录剪切/移动 | sec、Go FFI、真实 Dart FFI 和 widget 已覆盖加密名称、复制后删除、冲突与部分完成 | 在两个已解锁 root 间验证空/嵌套/中文目录，冲突的保留两者/替换/取消与批量队列；占用源目录时验证两端保留、可重试和提示。 |
| UI-59 | 窄窗口英文文件操作菜单 | widget 已证明约 256px 宽的操作标签可收缩省略，完整文本保留在 Semantics；list/grid 复用同一菜单路径 | 100%/125%/150% 缩放与窄窗口下验证英文菜单无溢出、标签可理解、焦点和读屏名称完整。 |

## macOS

| 原 ID | 验收范围 | 当前代码/测试证据 | macOS 实机待验证 |
|---|---|---|---|
| UI-01、UI-04、UI-09、UI-10、UI-14、UI-17、UI-20、UI-25、UI-28 | 文件浏览器右键、应用内剪贴板、root 会话和显示别名 | widget、真实 FFI 或临时目录测试已覆盖核心语义 | 鼠标次键、键盘菜单、焦点高亮、大批次冲突、跨 root 页面、三种关闭语义、同名 root 别名和别名后解锁。 |
| UI-05、UI-06、UI-07、UI-18 | 设置宽屏、主题、未保存离开和默认项 | 布局/持久化/widget 已覆盖 | 缩放、系统主题切换、窗口关闭键和系统剪贴板。 |
| UI-08、UI-12、UI-13、UI-29、UI-32、UI-35 | 安全记事本、多窗口、草稿和查找 | root lease、broker、草稿、未知后缀只读和键盘 widget 已覆盖 | 子窗口首帧、关闭、同文件冲突、系统剪贴板、断电恢复、未知后缀与查找可见性。 |
| UI-26 | 图片查看器 | 真实 codec/FFI/WebP 与资源边界测试已覆盖 | 子窗口键盘/手势、平台 codec 差异和超大图片压力。 |
| UI-38、UI-42、UI-47、UI-49、UI-51 | 安全记事本查找、撤销重做、数字键盘 Enter 与脱敏错误 | 查找 overlay、标准 Ctrl/Cmd 快捷键、数字键盘 Enter、只读边界和脱敏错误 widget/controller 回归均已存在 | Retina 与缩放下验证查找可见居中与选中高亮、Cmd+F/Cmd+Z/Cmd+Shift+Z、普通/数字键盘 Enter、关闭/草稿/剪贴板错误的脱敏展示及 VoiceOver 状态。 |
| UI-40、UI-45、UI-50 | 图片查看器滚轮缩放与状态语义 | widget 已证明工具栏与鼠标滚轮共用 `10%–1000%` 缩放范围，并覆盖加载、拒绝、失败和已打开图片的 live region 语义 | 鼠标滚轮、触控板、Retina 下缩放下限、旋转、翻页和 VoiceOver 状态。 |
| UI-55 | 属性值选择与复制 | widget 覆盖安全值系统剪贴板复制、反馈和敏感字段无复制入口 | 鼠标选择、Cmd+C、复制按钮焦点/VoiceOver 标签，以及 Retina 文本截断。 |
| UI-58 | 跨 root 目录剪切/移动 | sec、Go FFI、真实 Dart FFI 和 widget 已覆盖加密名称、复制后删除、冲突与部分完成 | 在两个已解锁 root 间验证空/嵌套/中文目录，冲突的保留两者/替换/取消与批量队列，以及源删除失败时两端保留。 |
| UI-59 | 窄窗口英文文件操作菜单 | widget 已证明约 256px 宽的操作标签可收缩省略，完整文本保留在 Semantics；list/grid 复用同一菜单路径 | Retina、缩放与窄窗口下验证英文菜单无溢出、标签可理解、焦点和 VoiceOver 名称完整。 |

## 其它独立环境验收

| 原 ID | 验收范围 | 待验证内容 |
|---|---|---|
| UI-03、UI-21、UI-30 | 首次属性打开性能 | Linux 已用真实“右键菜单 → 属性”路径复现首次冷帧：文件属性约 `build_us=309136`、`total_us=336002`，root 属性约 `build_us=243498`、`total_us=248529`；完成一次属性挂载并等待卸载帧后，另一种属性降至 `build_us=381`、`total_us=8434`。根因是菜单 route 移除与属性 overlay 在同一帧触发首次初始化；HomePage 的文件/root 属性入口现先等待 `WidgetsBinding.instance.endOfFrame` 再插入 overlay。A/B smoke 中延后一帧后为 `build_us=507`、`total_us=7743`；未触发 FFI 或磁盘读取。仍需实际主界面鼠标路径和其它平台复验。 |
| UI-15、UI-16 | 浏览模式和当前目录筛选 | 深层目录懒加载、超大目录和不同缩放比；全 root 递归搜索仍是独立未实现功能。 |
| UI-19、UI-31 | Linux 子窗口关闭 | 当前产品决策采用软件渲染止崩和 `FlView` wrapper 留存到进程退出的已接受资源泄漏方案；恢复根因修复前先建立子窗口数量、内存上限和进程级回收策略。 |
| UI-38、UI-42、UI-47、UI-49、UI-51 | Linux 安全记事本交互与脱敏错误 | Linux 主线 widget/controller 回归已覆盖查找、撤销重做、数字键盘 Enter、只读边界和脱敏错误 | 在不同缩放下验证查找可见与选中高亮、Ctrl 快捷键、普通/数字键盘 Enter、关闭/草稿/剪贴板错误的脱敏展示和读屏状态。 |
| UI-40、UI-45、UI-50 | Linux 图片浏览器交互与状态语义 | Linux 主线 widget、codec 与真实 FFI 证据已覆盖缩放、旋转及加载/失败状态语义 | 在鼠标与触控板、高 DPI、不同缩放下验证滚轮缩放下限、旋转、翻页和读屏状态。 |
| UI-55 | Linux 属性值选择与复制 | widget 覆盖安全值系统剪贴板复制、反馈和敏感字段无复制入口 | 验证鼠标选择、Ctrl+C、复制按钮、读屏标签和长路径截断。 |
| UI-58 | Linux 跨 root 目录剪切/移动 | sec、Go FFI、真实 Dart FFI 和 widget 已覆盖加密名称、复制后删除、冲突与部分完成 | 在两个已解锁 root 间验证空/嵌套/中文目录，冲突的保留两者/替换/取消与批量队列，以及删除失败时两端保留。 |
| UI-59 | Linux 窄窗口英文文件操作菜单 | widget 已证明约 256px 宽的操作标签可收缩省略，完整文本保留在 Semantics；list/grid 复用同一菜单路径 | 不同缩放、高 DPI 与窄窗口下验证英文菜单无溢出、标签可理解、键盘焦点和读屏名称完整。 |
| UI-43 | 增量目录的筛选与排序边界 | walker 分页、list/grid 页顺序、已加载条目筛选、继续加载和 EOF 后排序已有 widget 与真实 FFI 证据；产品明确筛选不自动枚举未加载目录 | Windows/macOS/独立桌面环境验证大目录分页、筛选与排序边界、取消/导航/缩放下的交互和读屏反馈。 |
| UI-39、UI-44 | 应用隐藏与空闲 TTL 自动锁定 | Home/controller/bridge 已覆盖 per-root 计时、session gate、await 后 session 复核、窗口拒绝、迟到回复、保存排空和清洁窗口关闭；不满足安全关闭条件时保留 root | Windows/macOS/Linux 实际验证 `hidden`/`paused`/TTL 生命周期、窗口消失、部分关闭失败、真实 FFI close/reopen 及重新解锁后的新 session 不被旧锁定覆盖。 |
| UI-41 | Flutter UI 中文文案低频页面与可访问性 | 主工具栏、批量菜单、侧边栏、设置、图片浏览器、记事本、路径选择、导入和欢迎页的主要文案已统一；错误建议已改为可执行步骤，并有中文 widget 回归 | Windows/macOS/Linux 低频状态页逐页复核，验证读屏语义、字号、截断和长文案布局。 |
| UI-54 | 用户可见文案迁移门槛 | 产品 `lib` 词法审计当前为 0 候选；ARB 占位符和主要中英文 widget 回归已覆盖，服务层不再直接返回展示句子 | Windows/macOS/Linux 逐页中英文交互、locale 透传、读屏、对比度、字号和截断验收。 |
| UI-46 | Flutter UI 文案通俗化复查 | 图片浏览器、记事本、文件操作、进度、导入/导出、属性和欢迎页文案已按中英文 ARB 呈现；技术术语已从用户提示中移除，静态审计为 0 候选，相关 widget 回归已通过 | Windows/macOS/Linux 低频页面人工复核，验证语气、读屏、字号、长文案截断、换行和 locale 视觉一致性。 |
| UI-65 | 文件界面多选与打开方式 | 单击/双击打开、Ctrl/Meta、Shift、鼠标框选、方向键、Home/End、全选、Esc 和 list/grid 共享选择状态已有 widget 回归；框选绘制已与虚拟列表解耦 | Windows/macOS/Linux 真实鼠标/键盘、触控和读屏验收；真实大目录拖选帧率与内存基线。 |
| UI-60 | 解锁期间切换目录的会话一致性 | HomePage/widget 已覆盖旧 session 关闭、新目录保持锁定和迟到结果隔离 | Windows/macOS/独立桌面环境验证真实侧边栏切换、窗口异常关闭和进程退出。 |
| UI-64 | 主界面复制/剪切快捷键 | widget 已覆盖选中项、最近右键目标和安全文件剪贴板状态 | Windows/macOS/Linux 实际键盘布局、焦点优先级和读屏验收。 |
| UI-66 | 多语言换行转义一致性 | ARB、设置页和侧边栏真实换行 widget 断言已存在 | Windows/macOS/Linux 不同字号、缩放和窗口宽度下验证换行、截断与读屏。 |
| UI-67 | 设置界面重新分类与响应式排版 | 设置保存、未保存离开、默认值和宽窄布局 widget 已覆盖 | Windows/macOS/Linux 大字号、缩放、主题切换和长文案截断验收。 |
| UI-69 | 安全记事本剪贴板监视位置 | 监视条底部布局、短文本截断、刷新、清空和关闭 widget 已覆盖 | Windows/macOS/Linux 宽窄窗口、大字号、系统剪贴板和读屏验收。 |
| UI-78 | 安全记事本桌面快捷键兼容 | 查找、保存、撤销/重做、`Ctrl/Cmd+H`、`Ctrl/Cmd+Y`、普通/数字键盘 Enter 和查找提交 widget 回归已覆盖；完整真实 FFI Flutter 回归为 302 项通过 | Windows/macOS/Linux 实际键盘布局、焦点、输入法提交和原生窗口验收。 |
| UI-79 | 主界面粘贴快捷键的平台修正 | `Ctrl+V` 与 `Cmd+V` 共用安全文件剪贴板路径，主页冲突粘贴回归和完整 Flutter 回归已通过 | Windows/macOS/Linux 实际键盘布局、焦点优先级和读屏验收。 |
| UI-80 | 主界面刷新快捷键的平台修正 | `F5`、`Ctrl+R`、`Cmd+R` 共用当前目录刷新路径，主页快捷键回归和完整 Flutter 回归已通过 | Windows/macOS/Linux 实际键盘布局、焦点和刷新中的窗口反馈验收。 |
| UI-81 | 主界面筛选快捷键 | HomePage 通过 `FileBrowserState.focusFilter()` 打开并聚焦当前目录筛选；筛选范围仍限当前已加载条目，文件浏览器/主页定向回归和完整 Flutter 回归已通过 | Windows/macOS/Linux 实际键盘焦点、输入法和筛选空态/继续加载反馈验收。 |
| UI-83 | 安全记事本编辑输入字节上限 | 编辑器接入 16 MiB UTF-8 字节限制，完整 Unicode 截断、composing 清理及 ASCII/多字节边界回归已覆盖 | Windows/macOS/Linux 实际粘贴、输入法 composing、超限反馈和大文本压力验收；未知大小读取仍不属于本任务。 |
| UI-52 | 多语言基础设施与中文/英文首批界面 | ARB 生成、locale 透传、主要页面文案迁移、稳定错误映射和英文 widget 已覆盖；词法审计为 0 候选 | Windows/macOS/Linux 验证安全记事本、图片浏览器及其它低频页面的英文视觉、字号截断、窗口 locale 透传和读屏语义。 |
| UI-48 | 可改密码加密目录创建与改密链路 | sec、Go FFI、Dart FFI、创建/改密 widget 和 staged 配置故障注入已有证据 | Linux/Windows/macOS 实机验证原子配置替换、跨进程锁、进程退出和创建/改密 UI。 |
| Flutter 正确密码打开后目录可见性 | 解锁后目录列表 | Linux 已用真实 root 完成重开与列表渲染；Windows/macOS 使用隔离 profile 重做正确/错误密码、无 marker root 和损坏 marker 流程。 |

## 迁移记录

- UI-01 至 UI-42 和 SEC-01 中仅剩其它系统或独立环境验收的部分，已从 `TODO.md` 迁入本文件。
- 2026-07-23：UI-45、UI-47、UI-49、UI-50、UI-51、UI-58 的代码和自动化证据已在主线确认，剩余均为跨平台或独立桌面验收，已从 `TODO.md` 迁入本文件；其中 UI-58 原有条目已在三平台表中，现只消除重复登记。
- 2026-07-23：UI-59 的 Flutter 布局修复和 list/grid 共享菜单路径已有自动化证据，剩余仅为窄窗口、高 DPI 与读屏实机验收，已从 `TODO.md` 迁入本文件；完成档案中同名的历史 FFI 测试更正为 `TEST-FFI-01`，避免任务编号冲突。
- 2026-07-23：UI-60、UI-64、UI-66、UI-67、UI-69 的代码和自动化证据已确认，剩余仅为其它平台或独立桌面环境验收，已从 `TODO.md` 迁入本文件；不改变其未完成平台验收状态。
- 2026-07-23：UI-78、UI-79、UI-80、UI-81、UI-83 的代码和自动化证据已确认，剩余仅为真实桌面键盘、焦点、输入法或压力验收，已从 `TODO.md` 迁入本文件；不改变其未完成平台验收状态。
- 2026-07-23：UI-43 的分页、筛选和排序代码及真实 FFI/widget 证据已确认，剩余仅为其它平台或独立桌面环境验收，已从 `TODO.md` 迁入本文件；不将“筛选已加载条目”误报为全目录搜索。
- 2026-07-23：UI-52 的本地化基础设施、主要文案迁移、英文 widget 和词法审计证据已确认，剩余仅为三平台视觉、字号、locale 透传与读屏验收，已从 `TODO.md` 迁入本文件；不把静态审计结果宣称为完整多语言产品。
- 2026-07-23：UI-41、UI-54、UI-65 的代码和自动化证据已确认，剩余仅为低频文案/读屏、三平台视觉与真实输入/大目录性能验收，已从 `TODO.md` 迁入本文件；不降低其验收范围，也不宣称功能已达 100%。
- 2026-07-23：清理 `TODO.md` 中重复的 UI-70/UI-71/UI-72/UI-74 登记，分别归并到本文件的 UI-65/UI-66/UI-67/UI-69；这些功能已有实现和自动化证据，当前只剩平台视觉、真实输入、读屏或性能验收。UI-68 与重复的 UI-73 保留在活跃清单，因小键盘问题仍未复现。
- 2026-07-23：UI-39、UI-44 的 Home/controller/bridge 代码和自动化证据已确认，剩余仅为真实窗口、FFI 和三平台生命周期验收，已从 `TODO.md` 迁入本文件；不把 session 模拟测试当作桌面实测。
- 2026-07-23：UI-46 的 ARB 文案批次、静态审计和中英文 widget 证据已确认，剩余仅为低频页面人工语气、读屏、字号、截断和三平台视觉验收，已从 `TODO.md` 迁入本文件；不把源码审计当作完整可访问性验收。
- 2026-07-23：UI-48 的 sec/FFI/widget 与 staged 配置故障注入证据已确认，剩余仅为三平台原子替换、锁和 UI 实机验收，已从 `TODO.md` 迁入本文件；不把交叉编译当作实机完成证据。
- 本次迁移不宣称功能达到 100%，也不移除 P0/P1/P2 中仍有实际设计、实现或安全边界缺口的任务。
